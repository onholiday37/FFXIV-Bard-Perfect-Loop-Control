# 1.0 自检与伤害评估

验收日期：2026-09-03。版本：1.0.0.0。范围：当前仓库实现、编译、纯逻辑测试、离线模拟与交付文件核对。

> **任何第三方插件都有可能导致封号，后果自负。开别怕，怕别开。**
>
> 结论：已完成1.0实现和多轮本地检查，可以交付游戏内验收；不能宣称“已实战验证、全局最优、绝对不溢出、零延迟或免封号”。

## 修正了哪些实质问题

1. 能力技执行必须同时通过原生冷却和状态检查。只有有意排队的GCD例外跳过“当前冷却必须为零”检查；不再把忽略CD的条件查询当作技能已经就绪。
2. 碎心/失血读取当前可用充能；“距离全满的时间”不再挡住已有充能。
3. 联合搜索单插、双插、等待和下一窗口，而非固定九天/碎心排序。资源末期估值与自然触发分支让序列可比较，但它仍是有限前瞻近似。
4. 旅神三层先清音调；两层比较九天补三层、等自然判定后清三层、必要时提前清两层。九天预测诗心与实际已到层数分离。
5. 贤者歌考虑九天/自然诗心的7.5秒充能缩减及满槽浪费。军神加速层不再错误当成可释放音调的资源价值。
6. 团辅顺序有独立保护，碎心不能无限挤掉团辅。切旅神即可补齐第三种尾声时，优先切歌收齐再最终乐章；不把两种尾声误当三种。
7. 纷乱、侧风、返场余音、共鸣、爆破、绝峰均参与决策。高价值GCD触发临近过期可在增益外救回，不因猛者未开启永久卡住。
8. DoT按持续伤害计算，刷新与补挂扣除替代GCD成本；服务器跳伤相位未知时用期望。提前快照必须先观察到本地角色DoT刷新后的状态，不把客户端接受动作直接当成状态已经落地。
9. 观察所有本地执行，包括手动能力技/物品，共享双插预算；等待真实执行，不把排队的返回值算成伤害命中。
10. 原生明确拒绝后可重新选择其他候选；还差几毫秒转好属于等待，不进入失败重试冷却。未知执行结果超时则停止，不盲目重发。
11. 停止、倒地、换职业、目标失效与窗口X有保护。旧目标恢复时实时重算，不追赶旧秒表；玩家选择其他对象优先，不乱抢目标。
12. 另有“仅影子观察”开关；该模式执行器不发送技能，但第三方插件风险仍存在。

## 自动检查覆盖

最终一次记录：**Release构建0警告、0错误；1893项断言通过**。断言数量包含200组随机状态产生的多项不变量检查，不等于1891个独立实战案例。

| 讨论点 | 已完成的验证 | 尚需实际游戏验证 |
|---|---|---|
| 九天真实15秒CD、碎心已有充能可用 | 原生调用参数审查；充能数学测试；冷却中九天不挡碎心用例 | 本机SDK接口实际读取与游戏补丁兼容 |
| 三层音调→九天；两层九天→音调 | 无自然触发、有自然触发、等待判定、层数确认用例 | 实际诗心到账延迟与高延迟帧表现 |
| 贤者充能、魂音、军神加速 | 资源模型与随机状态测试；军神非音调资源回归 | 所有实际网络时序 |
| 团辅、第三尾声、纷乱、侧风、碎心 | 团辅保护、第三尾声、拒绝回退测试；全循环包含所有进攻能力技 | 实战团辅覆盖与队友增益配合 |
| 每GCD最多双插、最短间隔与动作锁 | 候选窗口不跨下一GCD；手动/自动共享预算模型 | 观察器是否正确覆盖本机所有手动执行路径 |
| DoT持续伤害与快照 | 短命目标、强旧快照、末段续时、两条状态分帧到账；模拟独立DoT跳伤时钟 | 服务端精确跳伤相位、完整队友快照 |
| 50级和升级技能 | 等级50/54/68/80/84/92/100随机过滤；低级技能名称 | 真正进入等级同步副本 |
| 上天、丢目标、恢复 | 无目标不出动作；固定20秒停火模拟；目标选择代码审查 | 各绝本机制、仍可选但免疫、换对象ID |
| X、stop、影子模式 | 执行入口代码复核，影子模式发送前直接返回 | 实际UI关闭与队列边界操作 |
| 交付版本 | DLL/清单版本与桌面、压缩包文件校验 | 用户将开发插件路径切换到新DLL |

低层调用观察的是**客户端执行**，不是服务器最终命中确认。成功读取到DoT状态刷新后才认可该次DoT快照；其他伤害没有冒充战斗日志确认。

## 离线伤害模型

每种配置10个固定随机种子，每次300秒；4种速度/歌轴 × 2种目标场景，合计80次1.0模拟与80次旧策略对照。停火场景固定为第78～98秒不可造成伤害。

对照不是“实际旧DLL的实测DPS”：它使用旧固定优先级，但修正了技能可用性，并与1.0使用相同的GCD/DoT决策器，避免把原有阻塞Bug带来的损失全部冒算成优化收益。

单位是**叠加所建模自身增益后的动作威力总量**，不是游戏伤害数值。未计自动攻击、装备换算、完整暴击直击随机、完整队友增益、真实网络/帧延迟。歌速包含军神及过渡加速；九天命中/诗心和DoT状态在模型中按0.65秒延迟生效；DoT刷新还预留了生效余量。这不是所有技能实际生效时间的统一承诺。模拟使用同一套纯GCD和能力技规划器，但没有运行原生执行器。

| 配置 | 目标状态 | 旧策略均值 | 1.0均值 | 模型变化 |
|---|---|---:|---:|---:|
| 2.47 / 3312 | 持续可攻击 | 63,346.8 | 66,028.1 | +4.23% |
| 2.49 / 3312 | 持续可攻击 | 62,868.5 | 65,705.2 | +4.51% |
| 2.50 / 3312 | 持续可攻击 | 65,179.5 | 66,322.4 | +1.75% |
| 2.50 / 369 | 持续可攻击 | 65,470.3 | 65,510.5 | +0.06% |
| 2.47 / 3312 | 中途停火20秒 | 60,617.3 | 61,900.4 | +2.12% |
| 2.49 / 3312 | 中途停火20秒 | 60,498.4 | 62,199.8 | +2.81% |
| 2.50 / 3312 | 中途停火20秒 | 61,683.6 | 62,107.0 | +0.69% |
| 2.50 / 369 | 中途停火20秒 | 61,534.7 | 62,442.2 | +1.47% |

2.49通用持续可攻击示例：300秒平均19.90次九天、36.80次碎心、16.10次音调；DoT平均贡献4,744.8模型威力，已经包含在65,705.2内，不能再重复相加。该组诗心溢出平均0.5层，**不是零溢出承诺**。有限插入位置、增益取舍和随机触发都可能使实际选择保留一部分溢出。

某个种子的起手：0.70旅神、1.40九天；3.19最终乐章、3.89战斗之声；6.18猛者；8.17碎心、8.87音调；10.66纷乱、11.36侧风；第二次九天16.40（与第一次相隔15秒）。这只是一个状态序列示例，不是强制固定动作表。

判断：通用轴在本模型中有改善，进阶369持续木桩几乎持平，不能宣称“进阶一定更强”。10个种子不是统计上的全局证明，样本最大值也不是理论上限。建议先使用通用3312完成实际验收。

## 性能

同机Release纯规划器30次采样：中位2.35ms，P95为3.95ms，最大4.33ms，见原始输出。此数据只覆盖纯规划计算，不包含游戏帧等待、原生状态读取、界面绘制和服务器往返，不能转述为端到端延迟一定小于50ms。

## 为什么仍不能叫“已证明完美”

- 有限前瞻，期末资源价值是近似，并没有穷举到战斗结束。
- 未来自然诗心随机，未来军神速度变化和实际效果到账存在误差。
- 不知道副本未来上天时间，除非用户提供预计可攻击时长；没有完整无敌状态表。
- 未把所有队友增益、自动攻击及真实装备属性纳入预测。
- 本次没有在用户游戏里加载新DLL、打木桩或参加副本。原生接口/钩子在运行期仍需验证。
- 已送进游戏队列的一发可能在stop/X后继续执行；插件只停止新增请求，不修改玩家队列。
- 版本1.0不代表无处罚风险。

## 最后自检流程

先检查核心规则和原生调用 → 编译并跑边界测试 → 跑160次全循环对照 → 修正团辅/尾声/资源/状态确认问题 → 再编译并完整重跑 → 桌面和压缩包逐文件版本/哈希核对。原始输出保存在 verification/test-results.txt；最终交付核对保存在 verification/delivery-check.txt。

参考：
[官方7.5吟游技能说明](https://na.finalfantasyxiv.com/jobguide/bard/)、
[The Balance 7.4基础指南](https://www.thebalanceffxiv.com/jobs/ranged/bard/basic-guide/)、
[ActionManager接口](https://github.com/aers/FFXIVClientStructs/blob/main/FFXIVClientStructs/FFXIV/Client/Game/ActionManager.cs)。
