# 1.1.6：复活后 Tab 选中了，不再因为晚一帧而一直等

> **任何第三方插件都有可能导致封号，后果自负。开别怕，怕别开。**

以前要求目标变化、可攻击状态和进战条件同一帧满足。选中那一帧如果距离不够或进战标记没更新，下一帧又会因为目标没变而继续等待。这是恢复逻辑的问题，不是你按错了。

现在会保留复活后的重新选敌记录。只要仍选中同一个目标，就等它可攻击、并且自己或目标处于战斗状态后恢复输出；不需要再次 Tab。等待期间不自动放技能或吃药食，状态窗显示“已记录重新选敌”。

取消目标会撤回记录，切换目标只等待新目标，再次死亡、手动停止或副本结束会清除记录。不会使用记忆中的另一个 Boss 攻击。没有重新选敌记录时，即使旧目标还在或重新可攻击，也不会恢复。

## 保留的安全边界

插件目前观察的是选中目标的变化，不直接监听 Tab 或鼠标按键。**首个复活帧仍只建立目标基线**，防止游戏恢复旧目标时误开火。因此恰好在这一帧选中，或同一旧目标一直没变，仍需等角色复活后取消再选一次。不能把自动恢复的目标当成明确的手动操作；本版没有假称解决这个边界，也无法区分其他插件替你切换目标。

这也不是新的开怪功能：正常开怪仍需手动进入战斗；团灭后仍需重新启动。复生保护也会受你自己的操作、自动攻击或其他插件影响，不能保证无敌。

## 检查

先加入“Tab 选中后下一帧进战标记恢复”的回归用例，在旧代码上确实失败；修复后通过。原来将延迟就绪后继续等待视为正确行为的两项测试已改正，并用独立场景覆盖。

新增检查覆盖进战标记延迟、走入射程、连续等待、取消选择、切换目标、再次死亡、旧目标残留、首个复活帧、停止与下一场战斗。取消和死亡会实际清空记录，不只是隐藏状态。

编译 0 警告、0 错误；5493 项检查、200 次常规模拟和 16 次长测通过。本次未改歌轴、双插、DoT 或药品时机，长期歌轴漂移仍按 1.1.5 说明保留。

**尚未游戏内实测。**离线检查验证的是恢复规则，不等于验证了你客户端的真实 Tab 事件与进战标记。请先确认加载路径是 1.1.6，再在适合测试的场景验证。

[完整测试输出](verification/test-results-1.1.6.txt)。
