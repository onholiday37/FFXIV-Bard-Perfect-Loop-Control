# 1.1.4：修复排队与 DoT 问题，复活后等你选目标

> **任何第三方插件都有可能导致封号，后果自负。开别怕，怕别开。**

## 排队技能取消后，不再一律停止

以前只要发出的技能超过两秒没有执行记录，就会停止。这个保护没有区分“可能已经执行但漏了记录”和“排队技能已经取消”，Boss 上天时就可能误停。

现在只对确实进入过游戏队列的 GCD 判断取消：队列需要持续为空至少 0.15 秒，并且经过原 GCD 的剩余时间和余量；期间动作序号不能变化，GCD 不能出现重新转动的迹象，也不能仍有施法或动作锁。满足这些条件后，丢弃这次待执行请求，保留启动状态，下一帧重新读取目标和循环。

这不会改写或强行清除游戏队列，也不会重复补发原来的技能。取消的 GCD 不计入已执行次数，更不会凭空多给两个插入位置。歌曲、用药计时和当前战斗时间都不因此重置。

如果队列还在，就继续等。如果出现无法解释的动作序号变化、GCD 重启、观察器异常或无效数据，仍按结果不明处理并停止。这项保护没有被删除。手动停止、团灭和战斗结束的已有设置也没有改变。

## 切目标期间的 DoT 按时间检查

原来是拿新读数直接减旧读数。例如上次剩 40 秒，切走 30 秒后回来剩 25 秒，代码只看到 40 变成 25，误以为是自然倒计时，可能继续使用旧的爆发增益。

现在每次观察都记录时间。上述情况如果没有刷新，应该只剩约 10 秒；实际却剩 25 秒，就说明旧记录不能再直接沿用。无法在确认窗口内对应到已观察技能时，快照标成未知，而不是猜成新增益或继续保留旧增益。

毒和风分别检查。只刷新毒不会无故清掉风的有效快照；真正只是自然倒计时，切走再回来仍会保留原记录。两种 DoT 分帧更新的已有确认逻辑也保留。

## 复活后不自动选中，也不偷偷打记住的目标

个人阵亡不关闭启动开关，但清掉插件记住的目标。复活后仍暂停技能、进攻团辅、歌曲和自动药食；你用 Tab 或鼠标重新选中可攻击敌人，才继续。旧目标残留时先取消再选中，不需要重新启动，也不用先手动打一招。

实现上看的是当前选中目标的变化，不监听或改绑按键。复活后的首帧只建立目标基线，防止游戏恢复旧目标时立即开火；之后只有目标发生变化、目标可攻击且战斗仍在进行，才解除等待。仅战斗标记恢复、旧 Boss 重新可攻击或点击同一个未改变的目标，都不会解除等待。选中队友、射程外或不可攻击的目标也不会恢复。

恢复后重新读取实际状态，不追补死前动作，不重置开怪与药品时钟；如果你已经手动打出了 GCD，不会再次清空它的插入计数。没有改变活着时 Boss 上天再返回的处理。

这是插件自身的暂停逻辑，不是游戏的无敌保证：你自己的动作、自动攻击或其他插件仍可能影响复生保护。插件无法区分其他插件改目标和手动选目标，请勿同时使用自动选敌功能。包内 1.1.2 说明属于历史记录，当前复活行为以本说明为准。

## 检查与本版范围

本地编译 0 警告、0 错误，5232 项检查通过，另完成 200 次、每次 300 秒的循环模拟。跨目标旧快照的复现测试在修复前失败，修复后通过。新增检查还覆盖复活后旧目标残留、300 帧持续等待、取消再选中、连续死亡、友方和不可攻击目标、复活首帧目标恢复，以及队列长时间保留、取消后恢复、动作序号回绕、GCD 重启证据、死亡/停止清理、不同排队窗口，以及 1 到 39 秒的 DoT 观察间隔。

旧测试中一组“未刷新的自然倒计时”读数没有扣足实际经过时间，本次改成了与时间一致的值；另用独立测试明确检查真正刷新必须失去旧快照，避免靠宽松样例掩盖错误。

本版没有改歌轴切点、最多双插、药品时机和群攻选择。自定义 GCD 被拒绝后的替代选择、无目标时的歌曲策略、低等级群攻缺失和长期歌轴延后，仍属于后续事项。

打包描述正常分段，避免纯中文单段被描述校验当成太短。没有上传 Git，也没有自动替换游戏正在加载的 DLL。

**尚未完成游戏内实测。**离线测试验证的是规则和边界，不能替代真实队列、回包和目标切换过程的验证，也不能据此承诺实战涨多少 DPS。[完整测试输出](verification/test-results-1.1.4.txt)随安装包附带。
