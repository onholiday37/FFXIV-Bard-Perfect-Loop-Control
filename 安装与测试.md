# 吟游诗人完美轴·完全控制版 0.4.0

> ⚠️ 本版会实际释放技能。任何第三方插件都有可能导致账号处罚或封号，后果由使用者自行承担。开别怕，怕别开。

## 安装

1. 将 `BardPerfectLoopControl.dll`、`BardPerfectLoopControl.json` 和 `BardPerfectLoopControl.deps.json` 放在同一文件夹。
2. 在 Dalamud 开发插件位置中添加 `BardPerfectLoopControl.dll` 的完整路径并加载。
3. 如果同时保留影子版，不要同时启动两边的循环。
4. 输入 `/brdcontrol config`，设置实际 GCD，并点选一个场景轴小按钮。
5. 默认使用“通用3-3-12”；2.49用户也可以直接点“2.49标准”，不会自动切到3-6-9。

## 场景按钮检查

1. 点“通用3312”或“2.49标准”：状态窗应显示43旅/43贤/34军，量谱切点2/2/11。
2. 点“2.50进阶”：应显示43旅/40贤/37军，量谱切点2/5/8。
3. 当前轴团辅应按光明神、战斗之声、下一 GCD 猛者的顺序；旧 NGA 对照模式仍按战斗之声、光明神、猛者。
4. 九天转好应优先释放；三诗音或“两诗音且九天即将转好”应先释放完美音调。
5. 当前轴只在安全刷新窗口更新DoT；只有旧NGA对照模式会使用猛者末段35秒截毒。

## 木桩首测

1. 先选中木桩并进入战斗，再输入 `/brdcontrol start`。
2. 状态窗应先显示 GCD 送入队列，实际 GCD 结算后显示“本周期 0/2”。
3. 能力技只能显示 `1/2`、`2/2`，达到 `2/2` 后必须等待下一个 GCD，不会发送第三个能力技。
4. 两个已接受动作之间固定至少相隔 0.70 秒；动画锁未结束或游戏已有动作排队时不会发送新能力技。
5. 输入 `/brdcontrol stop` 应立即停止自动执行。

## 50级同步测试

1. 进入有效等级50的日常副本后，状态窗应显示“等级同步 50：低等级自适应循环”。
2. GCD 只应出现强力射击、直线射击、毒咬箭和风蚀箭；不会尝试烈毒咬箭、狂风蚀箭、伶牙俐齿或辉煌箭。
3. 歌曲只使用贤者和军神，完整唱到接近结束后切换；两首都在冷却时允许暂时无歌。
4. 能力技只从当前等级已学习的猛者强击、战斗之声、纷乱箭和失血箭中选择。
5. 点击控制状态窗右上角 `X`，自动执行必须立即停止且窗口保持隐藏。使用 `/brdcontrol overlay` 可以重新显示。

## Boss 上天测试

1. Boss 变为不可选或从对象表消失时，状态窗应显示“目标不可选：停火”。
2. 停火期间不发送攻击，也不追赶已经错过的动作；歌曲和实际 CD 继续自然计时。
3. 同一个 Boss 对象重新可选时，会自动沿用并按复现时的 CD、歌、诗音和 DoT 重算。
4. 如果阶段转换生成了全新的 Boss 对象 ID，需要手动选中一次；插件不会自动选择场上的其他小怪。

## 命令

- `/brdcontrol start`：启动。
- `/brdcontrol stop`：立即停止。
- `/brdcontrol reset`：放弃当前周期并重新规划。
- `/brdcontrol config`：打开设置。
- `/brdcontrol overlay`：显示或隐藏状态窗。
