# 1.1.5：重新规划不再重开一遍起手

> **任何第三方插件都有可能导致封号，后果自负。开别怕，怕别开。**

## 重新对齐和切换方案

以前点重新对齐、输入 reset 或切换场景方案，会整体重置执行器。虽然战斗秒表和药品计时没动，已打出的 GCD 次数却归零了，规划器会短暂重新采用起手安排，尚未确认的技能也会被忘掉。

现在只丢弃旧建议，按新方案重新计算。GCD 次数、当前插入次数、动作间隔、技能拒绝后的短暂等待、排队请求和结果不明保护都保留。切方案不会凭空多出插入位置，也不会重发尚未确认的技能。

只有停止后重新启动，才是新的执行会话。死亡仍清理中断请求、保留本场计数，并等待你复活后重新选中敌人；本版没有恢复自动选敌。

## 自定义 GCD 不再反复占住第一名

自定义列表以前只暂时排除被拒绝的能力技，GCD 没有同样处理。第一项一直不能用时，下面能用的技能可能一直轮不到。

现在两种技能都遵守拒绝后的短暂排除。高优先级 GCD 暂不可用时，接着看你启用的其他 GCD；全部不可用就等待，不擅自加入关闭的技能。GCD 正常提前排队仍保留，不会因为公共冷却没转完而全被过滤。

## 切歌保留当前可用的位置

如果当前窗口能在配置切点之后合法切歌，会优先留位。有两插且音调、切歌都放得下时，仍先清音调再切。

若只剩一个位置，或两个动作已放不下，则按配置切歌，不再单纯为了留住诗心而推到下一 GCD。**这时可能放弃未清的诗心，是对切歌时点的优先保障，不代表每次都增加伤害。**诊断会说明这个取舍。

歌曲实际 CD、禁止提前切歌、动作锁和最多双插仍约束最终执行。窗口不够时继续等待，不强行卡 GCD，也不加第三插。

## 长期漂移没有完全解决

43/43/34 三段本身合计 120 秒。在不能提前切歌的前提下，每段又可能等到下一合法插入位置，这些等待会累积。仅修复插入位竞争，不能保证长期精确 120 秒一轮。

新增 16 次、每次 1200 秒的长测：2.48 通用轴两个样本中，旅神相邻轮次约隔 121.36～123.06 秒，第十次旅神相对严格 120 秒轴累计晚约 20.46～21.18 秒。原来 300 秒模拟中的 2.48 旅神仍在 0.70、122.28、244.74 秒开始。本次修复没有消除这些偏差。

进一步消除漂移，需要另行确定允许调整的歌轴窗口或其他取舍，本版没有偷偷改变预设切点。

## 检查与限制

编译通过：0 警告、0 错误。5452 项检查通过；原有 200 次、每次 300 秒模拟继续通过，本批伤害威力结果与 1.1.4 相同，不能据此承诺实战提升 DPS。另完成 16 次、每次 1200 秒长测，检查不提前切歌、不超出插入限制、歌循环不停止，并输出实际漂移。

新增检查包括：重新规划保留执行计数、插入预算、观察时间和待确认请求；重新规划后排队取消与结果不明；死亡、重新规划、选敌、恢复的连续状态变化；自定义候选拒绝与恢复、等级、群攻保护、条件及禁用技能。

这些是本地规则和状态流程检查，**尚未完成游戏内测试**。状态测试使用生产代码中的执行会话与恢复规则，但没有点击真实游戏窗口或验证网络回包。GCD 与能力技的完整联合收益搜索、实机事件回放和其他后续优化，本版没有一并宣称完成。

[完整测试输出](verification/test-results-1.1.5.txt)。旧版本说明属于历史记录，当前行为以本说明及 README 为准。
