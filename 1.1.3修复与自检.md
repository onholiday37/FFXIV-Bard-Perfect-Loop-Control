# 1.1.3：切歌修复与三个重点问题

> **任何第三方插件都有可能导致封号，后果自负。开别怕，怕别开。**

这次处理的是提前切歌的回归问题，也顺着执行、动作锁和持续伤害的记录检查了一遍。以下是这次确认、优先修复的三个问题，不代表整个项目已经没有其他问题。

## 1. 防断歌反而让歌提前切了

1.1.2 会根据下一次 GCD 和动作锁，把原本的切歌点往前挪。能力技排程里又额外提前了 0.25 秒。以 2.48 秒 GCD、0.70 秒动作锁和 0.08 秒余量计算，旅神或贤者剩约 3.51 秒就可能放行，早于当时设置的 2 秒。军神也有那 0.25 秒的提前量，但没有同样幅度的提前。

现在这两处提前都取消了。防断歌仍然会优先留位置，但不能改切歌点。发技能前还要重新读取当前歌曲，检查是不是该切的那首、时间是否已到，再交给游戏检查真实 CD 和技能可用状态。旧建议和自定义列表也不能绕过这个检查。

通用预设没有偷偷换轴，仍以唱 43 / 43 / 34 秒为目标，对应剩余 2 / 2 / 11 秒。这里不是说游戏计时一定要减一秒，而是在攻略的切歌区间里选了这些固定目标。[The Balance 基础攻略](https://www.thebalanceffxiv.com/jobs/ranged/bard/basic-guide/)给的是约 43 / 43 / 34 秒，以及旅神、贤者剩 3–2 秒，军神剩 12–10 秒的区间。

**到点不等于无条件马上发。**没有合法插入位、下一首没转好或没法攻击目标，就只能晚一些。绝神兵方案的阶段爆发调整仍然保留；它不等于严格的普通木桩歌轴。

之前的模拟没有走运行时那条“提前防断歌”计算，漏掉了这个问题。这次计时规则已共用，并增加切歌前 1 毫秒、0.1 秒、0.249 秒、不同速度、不同诗心层数和单插窗口的检查。模拟每次发歌也检查当前切歌期限。

## 2. 一次异常动作锁可能拖慢后面整场

以前记录的是启动以来最大的动作锁，只增不减。一次偏大的观测值会一直留下，导致后续原本能插的技能被判定放不下。

现在取最近 16 次、且不超过 30 秒的普通动作样本。相同动作跨多帧的观测只算一次，旧的异常值会退出。药品和无法识别的动作不混进普通动作样本；重新启动或倒地后也会清掉旧估计。

这不是取消延迟保护：配置中的动作锁下限、游戏当前动作锁、至少 0.70 秒的间隔和最多双插的限制都还在。持续高延迟仍会减少可用插入位，不保证任何网络条件下都不卡 GCD。

## 3. DoT 消失或刷新后，旧增益记录可能没有失效

旧版按目标保存毒、风的增益快照，但持续伤害消失、或者出现无法对应到已观察施放的刷新时，没有及时撤销旧记录。后面计算伶牙续毒收益，就可能把上一次爆发增益误当成现在的增益。

现在分别处理毒和风。哪一个消失，哪一个的旧快照就失效；不明来源的刷新也不会继承旧增益。只有观察到对应技能和持续时间更新，才确认新快照。两种 DoT 的更新分帧到达时，仍可在确认窗口内合并确认。

持续伤害仍参与收益计算。未知快照只是“不确定”，不会被当成已确认的旧爆发快照。

## 检查结果与边界

本地编译通过，0 警告、0 错误。4780 项自动检查通过；200 次、每次 300 秒的模拟完成，覆盖 2.47、2.48、2.49、2.50 秒 GCD，通用和进阶歌轴，以及有无失去目标的情况。

新增测试在修复前确实报出了提前切歌和过期 DoT 快照问题。已有复活等待、药食、群攻保护、九天与音调顺序、碎心充能和最多双插的检查仍然通过。[完整输出](verification/test-results-1.1.3.txt)可供核对。

2.48 秒木桩模拟的一组记录是：0.700 秒旅神，43.700 秒贤者，87.500 秒军神，122.280 秒再开旅神。后三次切歌时当前歌分别剩 2.000、1.200、10.220 秒。它说明没有提前，但也说明插入位置会造成延后；不能据此宣称已经实现精确 120 秒一轮或全局最高伤害。

**没有完成游戏内实测。**离线模拟不会验证真实网络回包、量谱更新延迟、实际技能命中或复活过程，也不能直接换算 FFLogs DPS。建议先用影子模式看建议，再决定是否自动输出。新诊断会带歌曲剩余时间、距切歌点的时间和动作锁，出问题时复制出来更容易定位。
